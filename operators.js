/*
1. Assignment operators
2. Comparison operators
3. Arthimetic operators
4. Bitwise operators
5. Logical operators
6. String opeartors
7. Conditional (ternary) operator
8. Comma operator
9. Unary operators
10. Relational operators
*/

// Assignment operator

let x = 10;
let y;

y = x;

console.log(y);

// Arithmetic operators

// +, -, *, /, **

// Logical operators

// &&, ||, !

// conditional operators (ternary operators)

const userRole = "admin";

if (userRole === "admin") {
  console.log("you are an admin");
} else {
  console.log("not admin");
}

// ternary operator
//  condition ? if-block : else-block;

// Comparison operators

//  ===, !==, ==, >, <, <=, >=

// data converting process is called as "coercion"
//      eg: 4 == '4' -> coercion will happen(datatype will change)
//      eg: 4 === '4' -> coercion will not happen
